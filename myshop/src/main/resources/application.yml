spring:
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:mysql://localhost:3306/myshop?serverTimezone=JST
    username: root
    password: mysql

mybatis:
  #mapperLocations: classpath*:/mapper/*.xml
  configuration:
    mapUnderscoreToCamelCase: true

logging:
  level:
    org:
      springframework: WARN
    jp.co.internous.myshop.model.mapper: DEBUG
